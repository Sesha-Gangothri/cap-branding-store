spring:
  application:
    name: booking-service
  datasource:
    url: jdbc:mysql://localhost:3306/sprint_project
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8085
  error:
    include:
      message: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8090/eureka